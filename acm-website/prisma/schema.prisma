generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Event {
  id        int   @id @default(cuid())
  eventName     String
  eventDate      Date
  eventLocation     String?
  eventDescription      String?
  eventImage  String?

  @@map("events")
}
